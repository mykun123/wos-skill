# wos-skill
An automated Web of Science batch export tool built on OpenClaw's opencli browser automation. It automates the entire WOS export workflow — keyword search, paginated batch export (auto-split into 1,000-record chunks), download verification, and final file merging — all through a single shell command.
